import { mk_reducer, action, simple_selectors } from 'rc-modules/redux';

export const initial_state = {}

export const actions = {};

export const reducer = mk_reducer(initial_state, {});

export const selectors = simple_selectors(initial_state);
